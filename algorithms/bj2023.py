


def recursion(prime_number, depth):
    global N
    samples = ['1', '3', '5', '7', '9']
    if depth == N:
        print(prime_number)
    for sample in samples:
        if len(prime_number) > 1:
            
            
        

N = int(input())